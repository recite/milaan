__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.ruff_cache/
dist/
build/
*.egg-info/
.DS_Store

# Generated per-run artifacts. Case data.csv files are regenerated by data.py
# and are reproducible from it, so they are not tracked.
cases/**/data.csv
cases/**/results.*.json
reports/*.md
reports/*.json
!reports/.gitkeep

# Track B harvest cache: large, re-fetchable, and pinned by content hash.
data/cache/

# Extracted corpus artifacts: large, and regenerable from softverse via
# `kasauti frame`. The derived frame and changes tables are tracked.
data/frame/call_sites.csv
data/frame/extract.log
dist/
